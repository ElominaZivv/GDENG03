#include "MainGame.h"

MainGame::MainGame(const jazzy::GameDesc& desc) : jazzy::Game(desc)
{
}

void MainGame::onCreate()
{
	Game::onCreate();
	// Place create the initial objects here


}

void MainGame::onUpdate(jazzy::f32 deltaTime)
{
	Game::onUpdate(deltaTime);
}
